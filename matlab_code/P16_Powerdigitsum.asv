clear;clc;
tic
x = 2^1000;
y = 0;
while(x > 0)
    y = rem(x,10) + y;
    x = floor(x/10);
end
disp(y);
toc


number = sym(2^x); 
n = char(number); 
y = 0; 
for i = 1:length(n) 
y = y + str2double(n(i)); 