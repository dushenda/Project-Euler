clear;clc
a=999;b=999;
c=num2str(a*b);
creverse=c(end:-1:1);
while(~strcmp(c,creverse))
    if(a*b<(a-1)^2)
        b=b-1;
        c=num2str(a*b);
        creverse=c(end:-1:1);
    else
    end
end
disp(['a=',num2str(a),',','b=',num2str(b),',','a*b=',c]);